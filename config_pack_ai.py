# Configuración de Pack AI

# Si es True, permite incluir archivos .env.example, .env.sample, .env.template
# (Siguen siendo escaneados en busca de secretos antes de incluirse)
INCLUDE_ENV_EXAMPLE = True

# Si es True, incluye lockfiles de dependencias conocidos por defecto.
# Se puede invertir puntualmente con --no-lockfiles.
INCLUDE_LOCKFILES = True
